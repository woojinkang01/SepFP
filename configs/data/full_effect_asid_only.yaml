_target_: sepfp.data.datamodule.SepFPDataModule

dataset:
  data_path: /home/maclab/user_woojinkang/SepFP/data/multistem/6stem/moisesdb/audio_16k_mono_cropped
  meta_path: /home/maclab/user_woojinkang/SepFP/data/multistem/6stem/moisesdb/meta_train208_seed0
  stems: ${stem_set.stems}
  duration: 7.2
  sample_rate: 16000
  board:
    - effect:
        _target_: pedalboard.PeakFilter
      p: 0.2
      cutoff_frequency_hz: "uniform 440 50 8000"
      gain_db: "uniform 0 -20 10"
      q: "random 1. 0. 1."
      repeats: 8

    - effect:
        _target_: pedalboard.Gain
      p: 0.35
      gain_db: "uniform 0 -10 10"

    - effect:
        _target_: pedalboard.Compressor
      p: 0.2
      threshold_db: "uniform 0 -30 0"
      ratio: "choice 1 2 4 8 20"
      attack_ms: "uniform 0 1 100"
      release_ms: "uniform 0 50 1000"
  duplicate_dataset: 8
  threshold: 0.5
  subset: 1.0
  num_trials: 10

validation_dataset:
  data_path: /home/maclab/user_woojinkang/SepFP/data/multistem/6stem/moisesdb/audio_16k_mono_cropped
  meta_path: /home/maclab/user_woojinkang/SepFP/data/multistem/6stem/moisesdb/meta_val32_seed0
  stems: ${stem_set.stems}
  duration: 7.2
  sample_rate: 16000
  board: []
  duplicate_dataset: 1
  threshold: 0.5
  subset: 1.0
  num_trials: 10

norm_stats: [-4.532, 1.023]

dataloader:
  batch_size: 64
  num_workers: 3
  pin_memory: true
  persistent_workers: true
  devices: ${trainer.devices}
